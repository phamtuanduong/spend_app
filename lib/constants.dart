import 'package:flutter/material.dart';

const primaryColor = Color(0xFF1393F4);
const secondaryColor = Color(0xFFF6F8FA);
const darkColor = Color(0xFFF6F8FA);

const defaultPadding = 20.0;
const defaultOpacityBg = 0.1;
const defaultDuration =
    Duration(milliseconds: 500); // we use it on our animation
const maxWidth = 1440.0; // max width of our web