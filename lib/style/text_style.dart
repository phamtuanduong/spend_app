import 'package:flutter/material.dart';
import 'package:spend_app/style/colors.dart';

const textStyleSearch = TextStyle(
    color: menuUnselectedColor, fontSize: 13, fontWeight: FontWeight.normal);
final textSubStyle =
    textStyleSearch.copyWith(color: menuUnselectedColor, fontSize: 14);

final textDetailStyle =
    textStyleSearch.copyWith(color: Colors.black, fontSize: 17);

final textCoinStyle =
    textStyleSearch.copyWith(color: const Color(0xFF2196f3), fontSize: 18);
